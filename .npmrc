# Publish/consume @quynhonsemiconductor packages via GitHub Packages.
# Workspace-internal @quynhonsemiconductor/* packages still resolve locally via the pnpm
# workspace; this scope registry only applies to published resolution.
@quynhonsemiconductor:registry=https://npm.pkg.github.com
link-workspace-packages=true
prefer-workspace-packages=true
